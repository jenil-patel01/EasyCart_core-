@model List<eays.Models.ApplicationUser>
@{
    Layout = "~/Views/Shared/AdminLayout.cshtml";
}

<div style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 1.5rem;">
<h2 class="fw-bold mb-0" style="color: #2d3436; margin: 0;">Users</h2>
    
<!-- Search Form -->
<form method="get" asp-action="Users" style="margin: 0; display: flex; align-items: center; gap: 15px;">
    <div style="position: relative;">
        <i class="fa fa-search" style="position: absolute; top: 50%; left: 16px; transform: translateY(-50%); color: #9ca3af; font-size: 15px;"></i>
        <input type="text" name="search" value="@ViewBag.CurrentSearch" placeholder="Search users by email or name..." 
               style="width: 320px; padding: 10px 35px 10px 42px; border-radius: 50px; border: 1px solid #e5e7eb; background-color: #f9fafb; font-size: 14px; color: #4b5563; outline: none; transition: all 0.2s; box-shadow: 0 1px 2px rgba(0,0,0,0.03);"
               onfocus="this.style.backgroundColor='#ffffff'; this.style.borderColor='#d1d5db'; this.style.boxShadow='0 4px 6px rgba(0,0,0,0.05)';"
               onblur="this.style.backgroundColor='#f9fafb'; this.style.borderColor='#e5e7eb'; this.style.boxShadow='0 1px 2px rgba(0,0,0,0.03)';">
        @if (!string.IsNullOrEmpty(ViewBag.CurrentSearch as string))
        {
            <a href="/Admin/Users" style="position: absolute; top: 50%; right: 14px; transform: translateY(-50%); color: #ef4444; text-decoration: none; font-size: 16px;">
                <i class="fa fa-times-circle"></i>
            </a>
        }
    </div>
    <span style="font-size: 15px; color: #9ca3af; font-weight: 500;">
        @Model.Count user(s)
    </span>
</form>
</div>

<div class="card-table">

    <div class="table-header">
        <h3>All Users</h3>
    </div>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Full Name</th>
                <th>Email</th>
                <th>Address</th>
                <th style="text-align:right;">Actions</th>
            </tr>
        </thead>

        <tbody>
            @{
                int i = 1;
            }
            @foreach (var u in Model)
            {
                <tr>
                    <td>@i</td>
                   
                    <td>@u.FullName</td>
                    <td>@u.Email</td>
                    <td>@u.Address</td>

                    <td class="text-end" >
                       

                        <a href="/Admin/DeleteUser/@u.Id" class="btn-del">
                            Delete
                        </a>
                    </td>
                </tr>
                i++;
            }
        </tbody>
    </table>
</div>